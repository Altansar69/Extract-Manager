<Window x:Class="ExtractManager.ExtractionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Extraction" Height="400" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource WindowBackgroundBrush}"
        Foreground="{StaticResource TextBrush}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="Archive:" FontWeight="Bold" />
        <TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" x:Name="ArchiveFileTextBox" IsReadOnly="True"
                 Margin="0,5,0,10" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Output Directory:" FontWeight="Bold" />
        <TextBox Grid.Row="3" Grid.Column="0" x:Name="OutputPathTextBox" Margin="0,5,5,10" />
        <Button Grid.Row="3" Grid.Column="1" Content="..." Width="30" Click="BrowseOutput_Click" />

        <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Text="Output Log:" FontWeight="Bold"
                   Margin="0,0,0,5" />
        <ScrollViewer Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" VerticalScrollBarVisibility="Auto">
            <RichTextBox x:Name="LogRichTextBox" IsReadOnly="True" Background="Transparent" BorderThickness="0"
                         Foreground="{StaticResource TextBrush}">
                <FlowDocument>
                    <Paragraph x:Name="LogParagraph" />
                </FlowDocument>
            </RichTextBox>
        </ScrollViewer>

        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="StartButton" Content="Start" Width="80" Click="StartButton_Click" Margin="0,0,5,0"
                    Background="{StaticResource AccentBrush}" />
            <Button x:Name="CloseButton" Content="Close" IsEnabled="False" Width="80" Click="CloseButton_Click" />
        </StackPanel>
    </Grid>
</Window>